This is where you'd keep generic data models to be used as templates

Note that base_model_*.py models are not meant to be used directly, but rather as inherited by specific data models
On the other hand, model_*.py models are meant to be used as templates for fields within data models